<div class="padding-20">
  <p class="help">設定した推しタグの付いたGIFが表示されます。</p>
  <p>
    <% @tags.each do |tag| %>
      <%= link_to tag_path(tag_name: tag) do %>
        <span class="tag is-link">#<%= tag %></span>
      <% end %>
    <% end %>
  </p>
</div>

<%= render partial: 'gifs/gifgrid', locals: { gifs: @gifs } %>
